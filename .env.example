# Neptune Apex Configuration
APEX_HOST=192.168.1.100
APEX_USERNAME=admin
APEX_PASSWORD=1234

# InfluxDB 3.x Configuration
# Full URL to InfluxDB server (required)
INFLUX_URL=http://localhost:8086
# API token for authentication (required for InfluxDB Cloud, optional for local)
INFLUX_TOKEN=
# Database/bucket name to write to
INFLUX_DATABASE=aquarium

# Legacy config (backwards compatible, only used if INFLUX_URL not set)
# INFLUX_HOST=localhost
# INFLUX_PORT=8086

# Polling interval (cron expression)
# Default: every 5 minutes
POLL_INTERVAL=*/5 * * * *

# Force full sync - set to 'true' to write all records (fills gaps in data)
# InfluxDB will deduplicate existing records automatically
# Use this once to fill gaps, then remove or set to 'false'
# FORCE_FULL_SYNC=true

# Query lookback window in days (default: 30)
# InfluxDB 3 Core limits queries from scanning too many Parquet files
# This is the maximum age of data that will be queried during sync
# QUERY_LOOKBACK_DAYS=30

# Query chunk size in days (default: 7)
# Queries are performed in chunks to avoid scanning too many files at once
# Reduce this value if you still get "exceeding the file limit" errors
# QUERY_CHUNK_DAYS=7

# Grafana Configuration (Docker only)
# Admin password for Grafana web UI at http://<host>:3000
GRAFANA_PASSWORD=admin
